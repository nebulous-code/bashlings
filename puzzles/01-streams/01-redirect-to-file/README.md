# Save command output to a file

There are some files in `/puzzle/data/`. Run `ls /puzzle/data` and capture its
output (the file listing, one filename per line) into a file at
`/puzzle/out.txt`.

When you're done, `exit` the shell and `check.sh` will inspect `out.txt`.
