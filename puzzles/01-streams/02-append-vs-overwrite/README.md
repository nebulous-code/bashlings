# Append a line without clobbering the file

A log already exists at `/puzzle/notes.log` with three lines in it:

```
alpha
beta
gamma
```

Add a fourth line `delta` to the file **without removing** the existing lines.
The wrong operator will overwrite the file.
