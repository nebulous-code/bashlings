# Count the lines in a file

`/puzzle/data.txt` contains some lines of text. Write *just the count* (the
integer, with no leading spaces and no filename) into `/puzzle/count.txt`.

So if `data.txt` has 7 lines, `count.txt` should contain `7\n`.
