# Combine the tail of one file with the head of another

Inside `/puzzle/` you'll find `a.txt` and `b.txt`.

Write the **last 20 lines of `a.txt`** followed immediately by the **first
5 lines of `b.txt`** into a new file `c.txt`. No blank line between them. No
extra trailing content.
